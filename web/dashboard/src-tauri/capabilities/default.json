{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Bluei Edge dashboard 기본 권한 — core API 만 허용",
  "windows": ["main"],
  "permissions": ["core:default"]
}
